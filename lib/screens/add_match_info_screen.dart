import 'package:flutter/material.dart';

class AddMatchInfoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('AddMatchInfoScreen'),
      ),
    );
  }
}
